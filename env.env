# Токен Telegram-бота, полученный через BotFather. Этот токен необходим для аутентификации бота в Telegram API.
TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# Режим работы бота: укажите "polling" для long polling или "webhook" для использования вебхуков.
BOT_MODE=polling

# Публичный URL для вебхуков. Используется, если BOT_MODE установлен в "webhook".
# Убедитесь, что URL доступен извне и корректно настроен (например, с SSL-сертификатом).
WEBHOOK_URL=https://yourdomain.com/telegram

# Адрес и порт для прослушивания входящих вебхуков.
# Пример: ":8443" означает, что сервер будет слушать на порту 8443 на всех интерфейсах.
LISTEN_ADDR=:8443

# Интервал для long polling (в секундах). Определяет задержку между запросами в режиме polling.
POLL_INTERVAL=2

# Режим отладки: установите "true" для включения подробного логирования (полезно при разработке и отладке).
DEBUG=false

# Конфигурация теста:
# TEST_QUESTIONS - количество вопросов, которое будет предложено кандидату в тесте.
# TEST_DURATION  - время на прохождение теста (в минутах).
TEST_QUESTIONS=30
TEST_DURATION=25

# Тип хранилища состояний: "memory" для хранения в оперативной памяти, "json" для файлового хранилища.
STORAGE_TYPE=json

# Список администраторов (Telegram ID), разделенных запятой. Эти пользователи будут иметь административные привилегии.
ADMIN_IDS=123456789,987654321
